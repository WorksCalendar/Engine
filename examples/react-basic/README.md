# react-basic

Smallest end-to-end example: a month grid that highlights any day with a
rule violation. ~50 lines of consumer code in [`src/App.tsx`](./src/App.tsx).

## Run

```bash
# from the engine package root:
npm install && npm run build
cd examples/react-basic
npm install
npm run dev
```
